<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/menuFresh"
        android:icon="@drawable/ic_refresh"
        android:title="Refresh"
        app:showAsAction="always" />

    <item android:id="@+id/new_size"
        android:title="Choose new size"
        app:showAsAction="never"/>

    <item android:title="Create custom game"
        android:id="@+id/mi_custom"
        app:showAsAction="never"/>

    <item android:title="Download custom game"
        android:id="@+id/mi_download"
        app:showAsAction="never"/>
</menu>